#!/usr/bin/env ruby
require "bundler/setup"
$LOAD_PATH.unshift File.expand_path("../lib", __dir__)
require "friday"

# Set up project local directory automatically
Friday::Project.setup

Friday::Cli.start(ARGV)
